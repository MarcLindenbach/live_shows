const INITIAL_STATE = {
  shows: [],
};

const liveShowsReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default liveShowsReducer;
