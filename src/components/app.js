import React from 'react';

import { ShowList, LoadMoreButton } from '../containers';

const App = () => (
  <div className="app container">
    <ShowList />
    <LoadMoreButton />
  </div>
);

export default App;
